# Smart Diet Planner - Frontend (React + Vite)

## Setup (Windows PowerShell)

```powershell
cd frontend
npm install
npm run dev
```

- Vite dev server: `http://localhost:5173`
- Configure API base via `.env` (optional):

```
VITE_API_BASE=http://localhost:8000
```









